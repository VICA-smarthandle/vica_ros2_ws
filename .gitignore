# ROS2 build outputs
build/
install/
log/

# 매핑 미리보기(map_preview_node)와 현재 지도 표식. 둘 다 실행 중에 생긴다.
# maps/ 자체는 추적하므로 이것만 따로 제외한다.
maps/_live/
maps/CURRENT_MAP

# Cartographer 원본 상태. pgm·png·yaml 과 달리 수십 MB 급 이진 파일이라 git 에
# 넣으면 저장소가 급격히 무거워진다. 주행에는 필요 없고(map_server 는 pgm 을 읽는다)
# 나중에 pure localization 을 검토할 때만 쓴다.
#
# **젯슨에만 남는다는 뜻이다.** 재설치하면 사라지므로, 쓸 일이 생기면 bag 과 함께
# 따로 백업해 둔다.
maps/*.pbstream

# Runtime generated TF trees
tf_trees/
frames.pdf
frames.gv

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/

# Colcon / test
.pytest_cache/
.coverage
htmlcov/

# Editor
.vscode/
*.code-workspace
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# ROS bags / large runtime data
*.db3
*.mcap
*.bag
*.bag.active

# AI / model / TensorRT large files
*.onnx
*.engine
*.plan
*.pt
*.pth
*.gguf
*.safetensors

# Temporary files
*.tmp
*.bak
*.old

# Secrets
.env
*.key
*.pem
id_rsa*
id_ed25519*

# External ROS2 dependencies managed by vica.repos
src/realsense-ros/
src/ydlidar_ros2_driver/

# nvblox / Isaac ROS 플러그인 소스는 Isaac ROS Docker workspace(isaac_ros-dev)에서
# symlink로 연결한다(머신 종속 절대경로 → 커밋 금지). Host Nav2가 NvbloxCostmapLayer와
# DistanceMapSlice를 로드하려면 이 소스가 있어야 한다. 복구:
#   ln -s ~/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_msgs   src/nvblox_msgs
#   ln -s ~/workspaces/isaac_ros-dev/src/isaac_ros_nvblox/nvblox_nav2   src/nvblox_nav2
#   ln -s ~/workspaces/isaac_ros-dev/src/isaac_ros_common               src/isaac_ros_common
src/nvblox_msgs
src/nvblox_nav2
src/isaac_ros_common
AGENTS.md
